\chapter{\implementationdevelopmentname}
\newpage
\begin{parskipenv}
\section{Diseño de la arquitectura de la herramienta}
La herramienta ha sido diseñada con un enfoque modular, estructurada en distintos componentes que colaboran entre sí. Esta arquitectura permite mantener un flujo de ejecución claro, así como facilitar futuras ampliaciones, modificaciones o mantenimientos del sistema.

El núcleo principal del sistema es el archivo \textbf{main.py}, encargado de orquestar todo el proceso de escaneo. Este script actúa como controlador central, invocando los módulos individuales contenidos en la carpeta \texttt{/modules}, cada uno de los cuales implementa una fase concreta del reconocimiento. El flujo general está guiado por condiciones lógicas que determinan qué módulos deben ejecutarse en función de las características del objetivo.

Cada uno de estos módulos está implementado como un archivo Python independiente. Éstos siguen una estructura similar en la cual se definen funciones internas auxiliares para realizar tareas concretas, y al final del archivo se incluye una función con el nombre \texttt{run\_nombre\_del\_módulo()}, que es la encargada de ejecutar el módulo completo y es invocada desde el \texttt{main.py}. Estos scripts almacenan sus resultados en la carpeta \texttt{/logs}, los cuales serán utilizados posteriormente para generar el informe. A modo de ejemplo se muestra el pseudocódigo del módulo \texttt{nmap.py}:

\begin{lstlisting}[language=Python, caption={Estructura general de un módulo}]
# modules/nmap.py
def initial_scan(target_ip):
    # Escaneo inicial de puertos
    ...

def extract_ports(target_ip):
    # Extrae los puertos abiertos
    ...

def hard_scan(target_ip, open_ports):
    # Escaneo profundo en los puertos abiertos
    ...

def clear_report(target_ip):
    # Guarda los resultados en /logs
    ...

def run_nmap(target_ip):
    print("[*] Running nmap module...")
    initial_scan(target_ip)
    open_ports = extract_ports(target_ip)
    hard_scan(target_ip, open_ports)
    clear_report(target_ip)
    print("[+] Nmap module completed.")
\end{lstlisting}

\medskip

La lógica del flujo de escaneo implementada en \texttt{main.py} puede resumirse en el siguiente pseudocódigo:

\begin{lstlisting}[language=Python, caption={Pseudocódigo del archivo \textit{main.py}}]
# Escaneo inicial
run_nmap(target)

# Si el objetivo no es una IP, se trata de un dominio
try:
    ipaddress.ip_address(target)
except ValueError:
    run_dns_recon(target)

# Condiciones en funcion de los puertos detectados
if port_80_open:
    run_web_modules(target)

if detected_cms == "wordpress":
    run_wordpress(target)

if detected_cms == "joomla":
    run_joomla(target)

if port_21_open:
    run_ftp(target)

if port_22_open:
    run_ssh(target)

# Generacion del informe final
run_generate_report(target, flag)
\end{lstlisting}

\medskip

Por otra parte, el archivo \textbf{app.py} actúa como servidor web, encargado de gestionar la interfaz gráfica. Este archivo implementa una API interna mediante Flask, que conecta la interfaz (cliente) con el backend (lanzando llamadas al \textbf{main.py} y otras funciones auxiliares). A través de esta API se pueden lanzar escaneos, interactuar con el modelo de inteligencia artificial, consultar resultados anteriores o generar informes.

La interfaz gráfica está implementada como una aplicación de una sola página (\textit{Single Page Application}). Se compone principalmente de un archivo HTML (\textbf{index.html}) y un archivo JavaScript (\textbf{script.js}) que gestiona la interacción dinámica del usuario. Este último se encarga de manipular los elementos del DOM (\textit{Document Object Model}) \cite{wikipedia_dom}, y realizar peticiones \texttt{fetch} a los endpoints de \textbf{app.py}. Por ejemplo, cuando el usuario pulsa el botón de ``Iniciar Escaneo'', se ejecuta una petición \texttt{fetch(/fullscan)} enviando los parámetros seleccionados por el usuario para lanzar el análisis completo.

La integración con el modelo de Gemini se realiza utilizando la librería de Python \texttt{google.generativeai}. Se definen tres endpoints clave:

\begin{itemize}
    \item \texttt{/chat}: permite enviar preguntas al modelo IA desde el chat integrado.
    \item \texttt{/next\_offensive}: genera automáticamente próximos pasos ofensivos a seguir.
    \item \texttt{/next\_defensive}: genera automáticamente recomendaciones defensivas.
\end{itemize}

Todos los endpoints siguen una estructura similar: reciben la petición, cargan el contexto del informe más reciente, construyen el prompt y obtienen la respuesta del modelo. La diferencia entre ellos radica principalmente en el prompt utilizado. En el caso de \texttt{/chat}, se envía la pregunta del usuario junto con el informe, mientras que en los otros dos no es necesario incluir la pregunta, ya que el prompt es fijo. El siguiente pseudocódigo muestra la estructura general de estos endpoints:

\begin{lstlisting}[language=Python, caption={Estructura general de los endpoints de IA}]
@app.route("/endpoint", methods=["POST"])
def ia_endpoint():
    # Configurar API token
    google.generativeai.configure(api_key=API_KEY)

    # Cargar el contexto del informe más reciente
    report = load_latest_report()

    # Construir el prompt para la IA
    prompt = f"Informe: {report}\n
               Pregunta: {request.json['question']}"

    # Llamar al modelo de IA
    model = genai.GenerativeModel("gemini-2.0-flash")
    response = model.generate_content(prompt)

    # Devolver la respuesta al cliente (endpoint /chat)
    return jsonify({"response": response.text})

    # Guardar la respuesta en md (endpoints
    # /next_offensive y /next_defensive)
    save_response_to_md(response.text)
\end{lstlisting}

\medskip

La generación de informes se realiza mediante el módulo \texttt{generate\_report.py}, el cual es invocado al final de \texttt{main.py}. Este módulo recoge todos los archivos de resultados generados por los submódulos (almacenados en \texttt{/logs}) y construye un documento PDF utilizando la librería \texttt{ReportLab}. La implementación de \texttt{generate\_report.py} se resume en el siguiente pseudocódigo:

\begin{lstlisting}[language=Python, caption={Pseudocódigo del archivo \textit{generate\_report.py}}]
# generate_report.py
def create_elements():
    # Crea los elementos del informe
    ...

def generate_report(target):
    # Crea el documento PDF con los datos guardados en /logs
    ...
    # Si bandera -p activa, se asocia a un proyecto
    ...
    doc = SimpleDocTemplate(path, pagesize=letter)
    for filename in os.listdir(logs_path):
        if filename.endswith(".txt"):
            write_to_pdf()
    ...

def run_generate_report(target, flag=None):
    generate_report(target, flag)
\end{lstlisting}

Esta arquitectura modular permite separar claramente la lógica de escaneo, la interfaz gráfica y la interacción con el modelo de IA, facilitando tanto el desarrollo como la depuración o sustitución de componentes individuales.

\subsection{Tecnologías empleadas}
El desarrollo de la herramienta se ha llevado a cabo mediante una combinación de tecnologías tanto en el apartado del frontend como del backend. A continuación, se describen las principales tecnologías utilizadas:

\begin{itemize}
    \item \textbf{Frontend}: Se ha utilizado \textbf{HTML}, \textbf{CSS} y \textbf{JavaScript} para el diseño de la interfaz gráfica. JavaScript cumple una doble función: por un lado, permite gestionar la lógica de la interfaz (cambio de vistas, interacción dinámica con los elementos), y por otro, se encarga de establecer la comunicación con el backend mediante llamadas a la API, utilizando el método \texttt{fetch()}.
    
    \item \textbf{Backend}: Toda la lógica del backend está implementada en \textbf{Python}. Los módulos individuales encargados de realizar tareas específicas del reconocimiento están escritos en este lenguaje. Además, se utiliza \textbf{Flask}, un \textit{framework} web desarrollado en Python, para construir el archivo \texttt{app.py}, el cual actúa como puente entre la interfaz gráfica y el núcleo del sistema. Este archivo expone los endpoints necesarios para que el frontend pueda lanzar escaneos, acceder a informes, gestionar proyectos o interactuar con el modelo de IA.
    
    \item \textbf{Módulos de escaneo}: Cada uno de los módulos del sistema implementa una fase específica del proceso de auditoría y está escrito en Python. Estos módulos utilizan herramientas externas de línea de comandos o mediante APIs, como \texttt{Nmap}, \texttt{Subfinder}, \texttt{Nuclei}, \texttt{WPScan}, \texttt{Hydra}, entre otras, descritas en el capítulo de metodología.
    
    \item \textbf{Modelo de inteligencia artificial}: Para la parte de asistencia automatizada se ha integrado el modelo \textbf{Gemini Flash 2.0}, accedido a través de la API oficial proporcionada por Google. Las llamadas al modelo y el procesamiento de sus respuestas se realizan desde el backend en Python, integrándose de forma transparente en la interfaz para facilitar al usuario recomendaciones, próximos pasos o análisis contextualizado de los resultados.
\end{itemize}

La figura \ref{fig:tecnologias} resume las tecnologías empleadas en el desarrollo de la herramienta.

\begin{figure}[H]
    \centering
    \hspace*{-0.4cm}
    \includegraphics[width=0.7\textwidth]{pictures/tablaTecnologias.png}
    \caption{Tecnologías empleadas en la herramienta}
    \label{fig:tecnologias}
\end{figure}

\subsection{Comunicación entre componentes}
La herramienta ha sido diseñada siguiendo un modelo de arquitectura en el que cada componente tiene una función específica y se comunica con el resto de forma estructurada. Esta separación de responsabilidades permite una mayor mantenibilidad y escalabilidad, facilitando la incorporación de nuevas funcionalidades o la modificación de las existentes sin afectar al conjunto del sistema.

El flujo de comunicación entre los distintos componentes puede resumirse de la siguiente forma:

\begin{itemize}
\item El usuario interactúa con la interfaz gráfica.
\item Cuando el usuario realiza una acción (por ejemplo, iniciar un escaneo), el archivo \texttt{script.js} lanza una petición \texttt{HTTP} mediante \texttt{fetch()} hacia un endpoint definido en \texttt{app.py}, el cual actúa como intermediario entre la interfaz y el backend.
\item El archivo \texttt{app.py} procesa la solicitud recibida y lanza el script \texttt{main.py}, pasándole los parámetros necesarios (objetivo, modo de escaneo, etc.).
\item El archivo \texttt{main.py} es el encargado de orquestar el proceso de escaneo completo. En función del tipo de objetivo y los puertos detectados, invoca secuencialmente los distintos módulos independientes ubicados en el directorio \texttt{/modules}, cada uno responsable de una fase concreta del reconocimiento.
\item Los resultados generados por cada módulo se almacenan en archivos intermedios y son utilizados más adelante para generar un informe en formato PDF mediante la librería \textbf{ReportLab}.
\item Por otro lado, cuando el usuario desea interactuar con el sistema de inteligencia artificial (por ejemplo, para solicitar recomendaciones o aclaraciones sobre el escaneo), la interfaz envía la petición a otro endpoint de \texttt{app.py}, que a su vez realiza una llamada a la \textbf{API de Gemini Flash 2.0}. La respuesta obtenida se devuelve a la interfaz y se muestra al usuario.
\end{itemize}

La Figura~\ref{fig:comunicacion_componentes} resume gráficamente este flujo de comunicación y la relación entre los distintos bloques funcionales del sistema.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{pictures/comunicacionComponentes.png}
\caption{Flujo de comunicación entre los componentes de la herramienta.}
\label{fig:comunicacion_componentes}
\end{figure}

Este diseño modular favorece la separación de responsabilidades y permite modificar o sustituir componentes concretos (como los módulos de escaneo o el motor de IA) sin afectar al resto de la aplicación, facilitando así su mantenimiento y evolución futura.

\section{Decisiones técnicas}
Durante el desarrollo de la herramienta se han tomado una serie de decisiones técnicas orientadas a maximizar la simplicidad, la eficiencia y la facilidad de mantenimiento del sistema. A continuación, se justifican las elecciones más relevantes respecto a tecnologías utilizadas y otras que fueron descartadas.

\subsection{Frontend: HTML, CSS y JavaScript}
Se optó por una interfaz basada en HTML, CSS y JavaScript puro por las siguientes razones:

\begin{itemize}
    \item \textbf{Simplicidad y ligereza:} al tratarse de una herramienta de propósito técnico, la interfaz no requiere grandes funcionalidades interactivas, por lo que el uso de tecnologías más avanzadas como React habría sido innecesario.
    
    \item \textbf{Facilidad de integración:} el frontend se comunica directamente con el backend mediante peticiones \texttt{fetch}, sin necesidad de routers, sesiones, ni gestión de estados complejos.
    
    \item \textbf{Rapidez de desarrollo:} la curva de aprendizaje es más baja y permite una implementación más directa y rápida.
\end{itemize}

\textbf{Tecnologías descartadas:} React (incluyendo Next.js) fue considerado inicialmente como posible alternativa. Sin embargo, su inclusión suponía una sobrecarga innecesaria en complejidad, ya que no se requerían características avanzadas como rutas dinámicas, autenticación, manejo de estado o conexión con bases de datos. Por ello, se optó por una solución más ligera y directa.

\subsection{Backend: Python y Flask}
La elección de Python como lenguaje principal y de Flask como framework se justifica por:

\begin{itemize}
    \item \textbf{Alta integración con herramientas de seguridad:} muchas de las utilidades utilizadas (Nmap, Subfinder, Nuclei, etc.) se invocan fácilmente desde Python mediante llamadas al sistema.
    
    \item \textbf{Desarrollo rápido y modular:} Flask proporciona una estructura clara, simple y fácil de mantener, ideal para proyectos de esta naturaleza.
    
    \item \textbf{Comunicación efectiva con el frontend:} al actuar como backend de la interfaz, Flask permite exponer endpoints REST a los que se puede acceder desde JavaScript con facilidad.
\end{itemize}

\subsection{Orquestación mediante \texttt{main.py}}
Se decidió centralizar toda la lógica de ejecución de módulos en un único script llamado \texttt{main.py}, lo que permite:

\begin{itemize}
    \item Encapsular la lógica del flujo de ejecución.
    \item Garantizar la reutilización del código tanto desde la consola como desde la interfaz.
    \item Permitir una estructura modular, desacoplando cada funcionalidad en archivos separados dentro del directorio \texttt{/modules}.
\end{itemize}

En conjunto, estas tecnologías han permitido desarrollar una herramienta modular y fácilmente extensible, sin recurrir a opciones innecesariamente complejas para el propósito de la misma.

\section{Diseño experimental}
Esta sección describe los pasos necesarios para realizar una auditoría utilizando la herramienta desarrollada.

\subsection{Requisitos previos}
Antes de ejecutar la herramienta, es necesario cumplir con una serie de requisitos previos, tanto a nivel de software como de red.

\begin{itemize}
  \item \textbf{Entorno de ejecución}: La herramienta ha sido diseñada y probada sobre sistemas operativos GNU/Linux, por lo que se recomienda su uso en distribuciones como Ubuntu, Debian o Kali Linux.

  \item \textbf{Python 3}: Es imprescindible contar con una versión de Python 3 instalada en el sistema. Se recomienda Python 3.8 o superior para asegurar la compatibilidad con todas las dependencias.

  \item \textbf{Instalación de dependencias}: Las librerías necesarias para ejecutar la herramienta se encuentran especificadas en el archivo \texttt{requirements.txt}. Se pueden instalar con el siguiente comando: \texttt{pip install -r requirements.txt}

  \item \textbf{Herramientas externas}: Algunas funcionalidades dependen de herramientas externas que deben instalarse manualmente en el sistema. Estas incluyen:

  \begin{itemize}
    \item \textbf{Nmap}
    \item \textbf{Subfinder}
    \item \textbf{Httpx}
    \item \textbf{Nuclei}
    \item \textbf{WhatWeb}
    \item \textbf{Feroxbuster}
    \item \textbf{WPScan}
    \item \textbf{Joomscan}
    \item \textbf{Hydra}
    \item \textbf{Searchsploit}
  \end{itemize}

  Es recomendable añadir estas herramientas al \texttt{PATH} del sistema para que puedan ser invocadas desde cualquier módulo.

  \item \textbf{Acceso a Internet}: La herramienta requiere conexión a Internet para funcionalidades que se apoyan en APIs externas, como la consulta WHOIS, OSINT de correos electrónicos o interacción con el modelo de inteligencia artificial.

  \item \textbf{Claves de API}: Para utilizar algunos servicios, como API Ninja o IntelligenceX, es necesario disponer de una clave de API válida. Estas claves pueden obtenerse a través de las páginas de cada servicio y añadirse desde la interfaz gráfica o directamente en el archivo de configuración correspondiente (en la carpeta \texttt{/config}).
\end{itemize}

\subsection{Configuración del escaneo}
La herramienta permite configurar distintos aspectos del escaneo, adaptándose así a múltiples escenarios y necesidades del auditor. A continuación, se describen las principales opciones de configuración disponibles:

\begin{itemize}
  \item \textbf{Objetivo del escaneo}: el usuario puede especificar como objetivo tanto una dirección IP como un nombre de dominio o una URL. La herramienta detecta automáticamente el tipo de objetivo y ajusta el flujo de reconocimiento en consecuencia.

  \item \textbf{Modo de ejecución - interfaz gráfica o consola}:
  \begin{itemize}
    \item \textit{Interfaz gráfica (GUI)}: para lanzar un escaneo desde la interfaz, el usuario debe acceder a la pestaña de escaneo, introducir el objetivo y pulsar el botón \texttt{Start}. Los resultados se irán mostrando dinámicamente en la consola de escaneo integrada.\\
    \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{pictures/1.png}
    \caption{Escaneo desde la interfaz gráfica.}
    \label{fig:gui_escaneo}
    \end{figure}
    
    \item \textit{Consola (CLI)}: también es posible lanzar un escaneo directamente desde la línea de comandos. Para ello, basta con ejecutar:
    \begin{verbatim}
    python3 main.py <objetivo>
    \end{verbatim}
    \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{pictures/2.png}
    \caption{Escaneo desde consola.}
    \label{fig:cli_escaneo}
    \end{figure}
  \end{itemize}

  \item \textbf{Escaneo exhaustivo}: esta opción permite aumentar la profundidad del análisis. Al activarse:
  \begin{itemize}
    \item Se lanza \texttt{Nuclei} para todos los subdominios activos, no solo para el dominio principal.
    \item Se ejecuta \texttt{Feroxbuster} con nivel de recursividad 2 (en lugar del 1 por defecto).
  \end{itemize}
  Desde la interfaz gráfica, se puede activar esta opción marcando la casilla correspondiente al iniciar el escaneo. Desde consola, se activa con el parámetro \texttt{-ex}:
  \begin{verbatim}
  python3 main.py <objetivo> -ex
  \end{verbatim}
  \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{pictures/exscan.png}
    \caption{Escaneo exhaustivo desde la interfaz gráfica.}
    \label{fig:ex_scan}
  \end{figure}

  \item \textbf{Asociación con proyecto}: una de las características principales de la herramienta es su sistema de proyectos, que permite agrupar los escaneos realizados sobre un mismo objetivo, gestionar versiones de informes y mantener un historial completo del análisis.
  \begin{itemize}
    \item En la interfaz gráfica, los proyectos se crean desde la pestaña \texttt{Proyectos}. Allí se puede introducir el nombre del proyecto y el objetivo.\\
    \begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{pictures/3.png}
    \caption{Formulario de creación de nuevo proyecto.}
    \label{fig:crear_proyecto}
    \end{figure}
    
    \item Una vez creado, se accede a una vista específica del proyecto con tres pestañas:
    \begin{itemize}
      \item \textbf{Consola de escaneo}: donde se lanza el reconocimiento y se visualiza el progreso.
      \item \textbf{Historial de informes}: permite consultar los informes generados en auditorías anteriores.
      \item \textbf{Chat con Orthrus}: chatbot integrado que utiliza inteligencia artificial para responder preguntas sobre el proyecto, sugerir próximos pasos o aclarar dudas sobre los resultados obtenidos.
    \end{itemize}
    \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{pictures/4.png}
    \caption{Consola de escaneo dentro de un proyecto.}
    \label{fig:proyecto_consola}
    \end{figure}

    \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{pictures/5.png}
    \caption{Historial de informes generados en un proyecto.}
    \label{fig:proyecto_informes}
    \end{figure}

    \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{pictures/6.png}
    \caption{Interacción con el asistente de IA desde un proyecto.}
    \label{fig:proyecto_ia}
    \end{figure}

    \item Desde consola, no es posible crear nuevos proyectos, pero sí asociar un escaneo a uno ya existente con el parámetro \texttt{-p}:
    \begin{verbatim}
    python3 main.py <objetivo> -p nombre_proyecto
    \end{verbatim}
  \end{itemize}

  \item \textbf{Configuración de proxy}: desde la pestaña de configuración, se puede habilitar un proxy para los escaneos, especialmente útil para evitar bloqueos. La herramienta permite elegir entre proxies \texttt{HTTP} y \texttt{SOCKS5}. Este proxy se integra en \texttt{WPScan}, redirigiendo su tráfico por la ruta indicada.\\
  \begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{pictures/7.png}
  \caption{Configuración del proxy desde la interfaz.}
  \label{fig:config_proxy}
  \end{figure}
\end{itemize}

\subsection{Flujo de uso de la herramienta}
A continuación se describe un ejemplo del flujo completo de uso de la herramienta, aplicando la opción más realista y completa: la realización del escaneo desde un proyecto previamente creado en la interfaz gráfica.

El primer paso consiste en crear un nuevo proyecto asociado al objetivo, introduciendo los datos correspondientes. Este proceso se realiza desde la pestaña de ``Proyectos'', tal y como se ha mostrado anteriormente en la Figura~\ref{fig:crear_proyecto}.

Una vez creado el proyecto, se procede a lanzar el escaneo desde la consola de escaneo asociada al proyecto. Previamente, el usuario puede haber configurado opciones como el modo exhaustivo o el uso de un proxy. El lanzamiento del escaneo desde el entorno gráfico puede verse en la Figura~\ref{fig:proyecto_consola}, presentada en el apartado anterior.

Una vez finalizado el escaneo, la herramienta genera un informe PDF automáticamente, accesible desde la pestaña de informes del proyecto. Un ejemplo de este informe se muestra en la Figura~\ref{fig:reporte_generado}.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{pictures/8.png}
\caption{Visualización del informe PDF generado.}
\label{fig:reporte_generado}
\end{figure}

A continuación, es habitual interactuar con la inteligencia artificial integrada en la herramienta. Esta interacción puede adoptar distintas formas:

\begin{itemize}
    \item \textbf{Generación de siguientes pasos ofensivos:} el usuario puede pulsar el botón correspondiente para que la IA sugiera cómo continuar con la auditoría desde un enfoque ofensivo, tal y como se observa en la Figura~\ref{fig:ia_ofensiva}.

    \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{pictures/9.png}
    \caption{Generación de siguientes pasos desde una perspectiva ofensiva.}
    \label{fig:ia_ofensiva}
    \end{figure}

    \item \textbf{Generación de siguientes pasos defensivos:} por otra parte, se pueden generar recomendaciones defensivas para mitigar las debilidades detectadas, como se observa en la Figura~\ref{fig:ia_defensiva}.

    \begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{pictures/10.png}
    \caption{Generación de recomendaciones desde una perspectiva defensiva.}
    \label{fig:ia_defensiva}
    \end{figure}

    \item \textbf{Interacción directa con la IA:} además de los botones predefinidos, el usuario puede realizar preguntas para resolver dudas específicas sobre el escaneo, como se muestra en la Figura~\ref{fig:ia_chat}.

    \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{pictures/11.png}
    \caption{Interacción directa con la IA integrada en la herramienta.}
    \label{fig:ia_chat}
    \end{figure}
\end{itemize}

Tras esto, el usuario cuenta con una base sólida de información obtenida durante la fase de reconocimiento, que le permite continuar con las siguientes etapas de la auditoría.

\end{parskipenv}